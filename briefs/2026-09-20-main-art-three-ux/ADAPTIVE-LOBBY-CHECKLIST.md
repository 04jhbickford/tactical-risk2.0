# Adaptive lobby / chrome checklist

METHOD class: **chrome / P1** (not rules). Dual-fork `.02`–`.10` stay in ship.

## D1–D5 addendum (dual-path.11)

| ID | Lock | Proof |
|---|---|---|
| **D1** | ≤767 / @390 keep sheet grammar. Compact seats, MAIN scroll, Confirm 56px, no side rails. | stills @390 + density numbers match `.6` |
| **D2** | ≥1024 map-dominant grow frame. Canvas insets to `--three-left` / `--three-right` / `--three-top`. Scale stays; frame grows. | stills @1280×800; map is the largest region |
| **D3** | Left **context** rail + right **actions** rail + compact top 48–56px. Never a full-width bottom sheet on desktop. | `#three-context` + `#three-bottom` left:auto |
| **D4** | Lobby multi-col on wide. Opaque desktop lobby — no tiny-map dashboard. | 3-col home / 2-col seats @1280 |
| **D5** | Label **Experimental UX**. Kill New UX / Three.js copy. Classic `/` queryless unchanged. | polish + dual-path-mode harness |

Hover OK on fine pointer. Denser targets OK on desktop. Phone keeps 44/56.
