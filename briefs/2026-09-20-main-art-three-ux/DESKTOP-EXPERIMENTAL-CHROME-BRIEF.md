# DESKTOP EXPERIMENTAL CHROME — Viz craft SoT LOCKED

Stamp tip: **V2.81.57-dual-path.11**. HOLD MERGE. Quiet James.

North star: **TWO layouts, one product.**

- **≤767 / @390:** keep sheet grammar — NO regress `.6` / `.8` / `.9` / `.10` mobile
- **≥1024:** map-dominant + side rails / multi-col lobby; **NEVER** phone UI scaled to 1280
- Viz gate viewport **1280×800**

## Steal cite (READY)

map center + left context rail + right actions rail + compact top ~48–56px; fixed map scale grow frame.

## Kill

- full-width bottom sheet on desktop
- tiny-map dashboard
- New UX / Three.js copy — label is **Experimental UX**

## READY fail-closed

stamp `.11` + stills @1280 lobby (and in-game if reached) **AND** @390 no-regress → then parent wakes Viz.

Classic `/` smoke no regress. Dual-fork rules from `.02`–`.10` unchanged.

See addendum **D1–D5** in `ADAPTIVE-LOBBY-CHECKLIST.md` + METHOD class (chrome / P1).
